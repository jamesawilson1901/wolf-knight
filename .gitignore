# Extracted asset packs — inspect locally, copy chosen files into ./assets.
asset-raw/

.DS_Store
node_modules/

# Mermaid Reef (mermaid/)
mermaid/node_modules/
mermaid/assets-source/
# mermaid/dist is committed: it is the deployable static build
